class Reaction {

    /**
     * 
     * @param {Number} id 
     * @param {Emoji} emoji 
     * @param {User} user 
     */
    constructor(id, emoji, user){
        this.id = id;
        this.emoji = emoji;
        this.user = user;
    }
}